import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Newton Physics Simulator | 3D Interactive Motorcycle Jump",
  description: "Experience Newton's Laws in 3D with this interactive motorcycle jump simulator. Built with Next.js, Three.js, and Rapier.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
